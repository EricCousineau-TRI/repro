# -*- mode: python -*-
# vi: set ft=python :

load("//tools/lint:lint.bzl", "add_lint_tests")

# This is to ensure we build the public targets using `bazel build //...`.
filegroup(
    name = "build_as_simple_test",
    testonly = 1,
    srcs = ["@footswitch//:build_as_simple_test"],
)

add_lint_tests()
