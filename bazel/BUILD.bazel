# WARNING! This will modify the test data if this is executed via `run` or generally
# outside of the sandbox (e.g. `test --spawn_strategy=standalone`).
py_test(
    name = "data_test",
    srcs = ["data_test.py"],
    main = "data_test.py",
    data = ["input.txt"],
)
